# =============================================================================
# DreamMoments - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
#   cp .env.example .env.local
#
# After setting DATABASE_URL, run migrations:
#   pnpm db:push        # (development) push schema directly
#   pnpm db:migrate     # (production)  apply generated migration files
# =============================================================================

# -----------------------------------------------------------------------------
# Database (required)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://user:password@localhost:5432/dreammoments

# Optional pool tuning
# DB_POOL_MAX=10
# DB_IDLE_TIMEOUT=30000
# DB_CONNECTION_TIMEOUT=10000
# DB_SSL=true

# -----------------------------------------------------------------------------
# Auth (required)
# -----------------------------------------------------------------------------
JWT_SECRET=your-secret-at-least-32-characters-long

# Google OAuth
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback
GOOGLE_CLIENT_SECRET=your-google-client-secret

# -----------------------------------------------------------------------------
# AI content generation (required for AI features)
# -----------------------------------------------------------------------------
AI_API_KEY=sk-...
# AI_API_URL=https://api.openai.com/v1
# AI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Stripe payments (required for payments)
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# -----------------------------------------------------------------------------
# Email via Resend (required for notifications)
# -----------------------------------------------------------------------------
RESEND_API_KEY=re_...

# -----------------------------------------------------------------------------
# Image storage via Cloudflare R2 (optional, falls back to base64)
# -----------------------------------------------------------------------------
# VITE_R2_UPLOAD_URL=https://your-worker.your-subdomain.workers.dev
# VITE_R2_PUBLIC_BASE_URL=https://pub-xxx.r2.dev

# -----------------------------------------------------------------------------
# Error monitoring via Sentry (optional)
# -----------------------------------------------------------------------------
# Client-side (exposed to browser)
# VITE_SENTRY_DSN=https://publickey@o123.ingest.sentry.io/456
# Server-side
# SENTRY_DSN=https://publickey@o123.ingest.sentry.io/456
